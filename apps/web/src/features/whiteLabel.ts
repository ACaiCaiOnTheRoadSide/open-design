/**
 * This deployment is the hosted white-label SaaS edition. Keep UI-only product
 * policy behind one switch so upstream/self-hosted behavior remains easy to
 * restore without deleting underlying capabilities.
 */
export const WHITE_LABEL_SAAS = true;
